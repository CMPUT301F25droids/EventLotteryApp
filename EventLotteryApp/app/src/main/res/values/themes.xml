<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EventLotteryApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary color for selected tab indicator -->
        <item name="colorPrimary">@color/selected_tab_color</item>

        <!-- Used for text/icon tint on selected state -->
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSurfaceVariant">@color/unselected_tab_color</item>

        <!-- Navigation bar color to prevent pink tint -->
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowLightNavigationBar">true</item>

    </style>
    <style name="AppButton" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@color/button_background</item>
        <item name="android:textColor">@color/button_text</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <style name="Theme.EventLotteryApp" parent="Base.Theme.EventLotteryApp" />

    <style name="Theme.EventLotteryApp.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.EventLotteryApp.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.EventLotteryApp.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Custom Date Picker Theme -->
    <style name="CustomDatePickerTheme" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/calendar_selected_bg</item>
        <item name="colorOnPrimary">@color/white</item>
        
        <!-- Header Background -->
        <item name="colorSurface">@color/white</item>
        <item name="colorSurfaceVariant">@color/white</item>
        
        <!-- Header Text -->
        <item name="colorOnSurface">@color/black</item>
        <item name="colorOnSurfaceVariant">@color/black</item>
        
        <!-- Calendar Grid -->
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/medium_grey</item>
        
        <item name="materialCalendarHeaderSelection">@style/CustomCalendarHeaderSelection</item>
    </style>

    <style name="CustomCalendarHeaderSelection" parent="Widget.Material3.MaterialCalendar.HeaderSelection">
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Custom Dialog Theme -->
    <style name="CustomDatePickerDialog" parent="Theme.Material3.DayNight.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.5</item>
    </style>

    <!-- ZXing Capture Activity Theme -->
    <style name="zxing_capture" parent="Theme.AppCompat">
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>